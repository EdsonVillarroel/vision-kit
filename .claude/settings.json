{
  "hooks": {
    "PostToolUse": [
      {
        "matcher": "Edit|Write",
        "hooks": [
          {
            "type": "command",
            "command": "python3 -c \"\nimport sys, json, subprocess\ntry:\n    data = json.load(sys.stdin)\n    fp = data.get('file_path', '')\n    if fp.endswith(('.ts', '.tsx')) and 'apps/frontend' in fp:\n        r = subprocess.run(\n            ['./node_modules/.bin/tsc', '--noEmit'],\n            cwd='/home/edsonv/Code/vision-kit/apps/frontend',\n            capture_output=True, text=True, timeout=30\n        )\n        if r.returncode != 0:\n            lines = (r.stdout + r.stderr).strip().split('\\\\n')\n            print('TS errors:')\n            print('\\\\n'.join(lines[:12]))\nexcept:\n    pass\n\""
          }
        ]
      }
    ]
  },
  "permissions": {
    "allow": [
      "Bash(npx supabase:*)",
      "Bash(./node_modules/.bin/tsc:*)"
    ]
  }
}
